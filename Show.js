
function Show(name, genre, avgRating, network, summary) {
  this.name = name;
  this.genre = genre;
  this.avgRating = avgRating;
  this.network = network;
  this.summary = summary;
}

module.exports = Show;
